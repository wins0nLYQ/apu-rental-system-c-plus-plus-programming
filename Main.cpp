/**
 * FILENAME: Main.cpp
 */

#include <iostream>

#include "Asia_Pacific_Home.h"
// #include "DoublyCircularLinkedList.h"

using namespace std;

int main()
{
    Asia_Pacific_Home APH; 
    APH.homePage();

    // DynamicArray<Property> properties;
    // ReadCSV read;
    // read.readCSV("mudah-apartment-kl-selangor.csv", properties);

    // DoublyCircularLinkedList<Property> x;
    // x.insertAtEnd(properties.get(0));
    // x.insertAtEnd(properties.get(1));
    // x.insertAtEnd(properties.get(2));
    // x.insertAtEnd(properties.get(3));
    // x.insertAtEnd(properties.get(4));

    // cout << x.getSize() << endl;

    // x.removeAtIndex(0);
    // x.removeAtIndex(0);
    // x.removeAtIndex(0);
    // x.removeAtIndex(0);
    // x.removeAtIndex(0);

    // cout << x.getSize() << endl;

    return 0;
}